# This is a template file - copy this to .env and fill in your actual values
# DO NOT commit .env file to git (it's in .gitignore)

# Copy backend/.env.example to backend/.env and configure there
# This root .env.example is for reference only
